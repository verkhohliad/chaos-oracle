# Dependencies for local mode (no ChaosChain Gateway / SDK).
# Mirrors agents/requirements.txt minus chaoschain-sdk.
web3>=6.0.0
aiohttp>=3.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=23.0.0
